# ============================================================================
# phix 官网（phix.ing）—— 绝不入库的东西
#
# 原则：**只上传本地的源码**。生产环境里的真实用户数据一律不进仓库，
# 与心履（xinlv-web 等）的做法一致。
#
# 注意：.gitignore 里 `#` 只有**在行首**才是注释。行尾写 `pattern  # 说明`
# 会把说明当成模式的一部分，那条规则等于没写（这个坑踩过一次）。
# 所以下面所有说明都单独成行。
# ============================================================================

# ---- 生产数据：含真实用户信息，绝不入库 ----

# CMS 文案 + 后台改动；deploy.py 也刻意不覆盖它，生产机有自己的那份
content.json

# 「PHIX 日志」：同学送修物品的记录与照片索引
logs.json

# 同学通过反馈表单提交的内容
feedback/

# 管理后台操作审计
admin_audit.log

# 旧账号哈希（心履账号迁移用）
.legacy_users.json

# ---- 密钥与站点私钥（每台机器自己生成）----

# HMAC 签名 cookie 用
.site_secret

# phix 服务端公钥（TOFU 固定）
.phix_pubkey

# 与 phix 服务端之间的机器间密钥
.service_key

# ---- 静态产物与二进制 ----

# 安装包（约 760 MB）走 GitHub Releases 与站点自身分发，不进 git
# media/logs/ 是用户上传的照片，属于用户数据
media/

# 自更新载荷（/updates/<产品>/ 的 exe、zip、latest.yml）同样是二进制产物：
# 由 deploy.py 上传到生产机，不进 git —— 单文件 163.9 MB 超过 GitHub 的 100 MB 上限
updates/
__pycache__/
*.pyc
test_results.txt

# ---- 本地开发杂项 ----
*.log
.venv/
venv/
